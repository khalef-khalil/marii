\chapter{Protocole expérimental et évaluation}
\label{chap:experimentation}

\section*{Introduction}
\addcontentsline{toc}{section}{Introduction}
Le chapitre~\ref{chap:conception} a fixé l'architecture HAKE-MER, les choix
de segmentation et d'extraction lexicale, ainsi que les hypothèses H1 à
H4. Ce chapitre en déduit le \textbf{contrat expérimental} : données,
hyperparamètres communs, drapeaux d'ablation, correspondance NRC et
statistiques de corpus \emph{mesurées} avant entraînement. Le protocole d'affinage est fixé (tableau~\ref{tab:expe-hyperparams}) ;
le Step~0 et les modules M1 à M4 sont évalués sous ce même contrat.
Les baselines PLM et les ablations +M1 à +M4 (H1 à H4) disposent de scores
test sur DistilBERT-base ; RoBERTa ne couvre que le Step~0 flat.

\section{Cadre expérimental}
\label{sec:expe-cadre}

Cette section fixe le corpus, les règles communes d'entraînement et les
encodeurs retenus pour que toutes les comparaisons ultérieures
portent sur le même cadre.

\subsection{Jeu de données}

Les expériences ciblent GoEmotions (configuration \emph{simplified})
\cite{demszky2020} : 43\,410 exemples d'entraînement, 5\,426 de
validation et 5\,427 de test, 27 émotions plus la classe \emph{neutral}
($K{=}28$), en multi-étiquettes. Les propriétés du corpus documentées
au chapitre~\ref{chap:etat_art} motivent le F1-macro, la prise en compte
du multi-étiquette et le prétraitement hiérarchique
(section~\ref{sec:conception-choix}).

\subsection{Analyse de corpus sous le protocole de segmentation}
\label{sec:expe-corpus-stats}

Avant tout entraînement, nous avons mesuré l'impact du découpage retenu
($M{=}4$ phrases, 32 tokens RoBERTa par phrase, segmentation NLTK
Punkt) sur les trois splits. Une analyse automatique du corpus,
menée avec exactement ces règles avant entraînement, fournit les
ordres de grandeur suivants.

Sur l'ensemble d'entraînement (même ordre de grandeur sur validation et
test) :
\begin{itemize}
\item environ \textbf{42\,\%} des commentaires comportent plus d'une
      phrase, ce qui justifie M1 ;
\item seulement \textbf{0{,}27\,\%} des exemples perdent plus de
      $M{=}4$ phrases (queue tronquée) ;
\item environ \textbf{0{,}74\,\%} des exemples requièrent une
      troncature à 32 tokens sur au moins une phrase conservée ;
\item environ \textbf{10{,}8\,\%} des tokens (alphabétiques) du texte
      retenu apparaissent dans le NRC Emotion Lexicon ;
\item environ \textbf{73\,\%} des documents contiennent au moins un
      token couvert par le NRC.
\end{itemize}
Ces ordres de grandeur indiquent que la troncature phrase n'est pas
dominante à ces bornes, tandis que la couverture lexicale laisse une
part non triviale d'exemples sans signal NRC au niveau token (d'où
l'intérêt de M3 et de l'analyse de $|g|$).

\subsection{Hyperparamètres communs}
\label{sec:expe-hyperparams}

Toutes les configurations comparées partageront le même budget
d'entraînement et la même métrique de validation (F1-macro sur le split
de validation). Pour l'affinage des encodeurs, nous ancrons le cœur du
budget sur la recette GoEmotions \cite{demszky2020} : lot
$16$, taux d'apprentissage $5\times 10^{-5}$, quatre époques d'entraînement
(la configuration officielle du jeu et le code associé retiennent
quatre époques pour BERT-base sur ce corpus). L'implémentation utilise
AdamW et un planificateur linéaire avec warmup (\emph{Transformers}
\cite{wolf2020}, pratique usuelle \cite{sun2019,devlin2019}) ; seuls
l'encodeur (DistilBERT-base, RoBERTa-base) et les modules M1 à M4
varient entre runs d'ablation. Le tableau~\ref{tab:expe-hyperparams}
fixe ces valeurs \emph{a priori} pour toutes les étapes.

\begin{table}[H]
\centering
\caption{Hyperparamètres communs (toutes configurations)}
\label{tab:expe-hyperparams}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{|p{4.2cm}|p{8.8cm}|}
\hline
\textbf{Paramètre} & \textbf{Valeur retenue} \\
\hline
Encodeurs & RoBERTa-base, DistilBERT-base \cite{liu2019roberta,sanh2019distilbert} \\
\hline
Optimiseur & AdamW, $\varepsilon=10^{-8}$ \\
\hline
Taux d'apprentissage & $5\times 10^{-5}$ (identique pour les deux encodeurs) \\
\hline
Poids de décroissance & $0{,}01$ \\
\hline
Taille de lot & $16$ (gradients accumulés si mémoire insuffisante) \\
\hline
Époques & $4$ (budget fixe, aligné GoEmotions \cite{demszky2020}) \\
\hline
Warmup & $10\,\%$ des pas d'optimisation \\
\hline
Sélection de modèle & Meilleur F1-macro validation sur les quatre époques \\
\hline
Graines & $42$, $123$, $456$ \\
\hline
Troncature (Step~0) & $128$ tokens document (alignée sur $4\times 32$ tokens, ch.~\ref{sec:conception-choix}) \\
\hline
Segmentation (M1+) & $M{=}4$ phrases, $32$ tokens/phrase \\
\hline
Seuil de décision & $0{,}5$ sur les sigmoïdes (sauf étude de seuil post hoc) \\
\hline
Métriques de test & F1-micro, F1-macro, exact match, mAP \\
\hline
\end{tabular}
\end{table}

\subsection{Implémentation modulaire et journalisation}
\label{sec:expe-impl-flags}

Une implémentation unique de HAKE-MER active ou désactive les modules
M1 à M4 et choisit la ressource lexicale (aucune, NRC, SenticNet).
Seuls ces choix diffèrent entre runs d'ablation ; le chargement des
données, les métriques et la boucle d'entraînement restent communs.

Chaque run enregistrera au minimum : identifiant de configuration,
backbone, graine, courbes train/validation, F1-macro et F1-micro
validation, checkpoint retenu, valeur finale de la porte $|g|$ (M3) et
statistiques par classe sur le test. Les comparaisons internes seront
interprétées uniquement \emph{à budget identique}.

\subsection{Backbones}

Deux encodeurs principaux : \textbf{RoBERTa-base} et
\textbf{DistilBERT-base}. Un essai BERT-base reste optionnel et hors
périmètre des comparaisons principales si le budget GPU est limité.

\section{Ablation cumulative}
\label{sec:expe-ablation}

L'évaluation principale reprend la progression du
tableau~\ref{tab:conception-ablation} sur chaque backbone. Le
tableau~\ref{tab:expe-ablation-flags} explicite les drapeaux.

\begin{table}[H]
\centering
\caption{Drapeaux d'ablation cumulative}
\label{tab:expe-ablation-flags}
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{|c|c|c|c|c|c|p{3.6cm}|}
\hline
\textbf{Étape} & \textbf{M1} & \textbf{M2} & \textbf{M3} & \textbf{M4} & \textbf{Lexique} & \textbf{Hyp.} \\
\hline
0 Baseline PLM & 0 & 0 & 0 & 0 & aucune & (réf.) \\
\hline
1 +M1 & 1 & 0 & 0 & 0 & aucune & H1 \\
\hline
2 +M2 & 1 & 1 & 0 & 0 & aucune & H2 \\
\hline
3 +M3 (NRC) & 1 & 1 & 1 & 0 & NRC & H3 \\
\hline
3 +M3 (SenticNet) & 1 & 1 & 1 & 0 & SenticNet & H3 \\
\hline
4 +M4 & 1 & 1 & 1 & 1 & NRC ou SenticNet & H4 \\
\hline
\end{tabular}
\end{table}

Seules les comparaisons adjacentes ou cumulatives à hyperparamètres
identiques seront interprétées comme preuve de l'apport d'un module.

\section{Correspondance NRC et GoEmotions}
\label{sec:expe-nrc-mapping}

Le module M3 (NRC) produit un vecteur $c \in \mathbb{R}^8$ correspondant
aux émotions Plutchik du lexique NRC \cite{mohammad2013}. Pour
documenter l'alignement taxonomique avec GoEmotions (H3), le
tableau~\ref{tab:nrc-goemotions-map} liste, pour chaque étiquette fine,
les dimensions NRC associées sémantiquement (relation many-to-one).
L'étiquette \emph{neutral} n'active aucune dimension NRC. Cette
correspondance est figée pour toute la campagne expérimentale ; le
vecteur injecté reste
\emph{partagé} entre émotions (section~\ref{sec:conception-m3}), la
table servant à l'analyse et à la justification du protocole.

\begin{table}[H]
\centering
\caption{Correspondance GoEmotions $\rightarrow$ émotions NRC (Plutchik)}
\label{tab:nrc-goemotions-map}
\small
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{|p{3.2cm}|p{10.8cm}|}
\hline
\textbf{Étiquette GoEmotions} & \textbf{Émotions NRC associées} \\
\hline
admiration & trust, joy \\
\hline
amusement & joy, surprise \\
\hline
anger & anger \\
\hline
annoyance & anger, disgust \\
\hline
approval & trust, joy \\
\hline
caring & trust, joy \\
\hline
confusion & surprise, fear \\
\hline
curiosity & anticipation, surprise \\
\hline
desire & anticipation, joy \\
\hline
disappointment & sadness, surprise \\
\hline
disapproval & anger, disgust \\
\hline
disgust & disgust \\
\hline
embarrassment & fear, sadness, surprise \\
\hline
excitement & joy, anticipation, surprise \\
\hline
fear & fear \\
\hline
gratitude & joy, trust \\
\hline
grief & sadness \\
\hline
joy & joy \\
\hline
love & joy, trust \\
\hline
nervousness & fear, anticipation \\
\hline
optimism & anticipation, joy, trust \\
\hline
pride & joy, trust \\
\hline
realization & surprise, anticipation \\
\hline
relief & joy, trust \\
\hline
remorse & sadness, fear, disgust \\
\hline
sadness & sadness \\
\hline
surprise & surprise \\
\hline
neutral & (aucune) \\
\hline
\end{tabular}
\end{table}

\section{Pipeline SenticNet (rappel protocolaire)}
\label{sec:expe-senticnet}

Pour H3, la branche SenticNet reprend les cinq descripteurs documentaires
fixés au chapitre~\ref{sec:conception-choix} : polarité moyenne, polarité
max, polarité min, fraction de tokens couverts, nombre de concepts distincts.
L'implémentation s'appuie sur SenticNet~5 \cite{cambria2018senticnet} avec
le même protocole de lookup que pour NRC (lemme puis surface).

\section{Comparaison des ressources externes (H3)}
\label{sec:expe-ressources-plan}

À architecture M1+M2 fixée, M3 activé et M4 désactivé, deux campagnes
diffèrent uniquement par la ressource lexicale (NRC vs SenticNet).
L'hypothèse H3 compare l'intégration de ces priors (alignement Plutchik
vs signal conceptuel) sans postuler a priori la supériorité de l'une
sur l'autre ni sur la pile M1+M2 seule.

\section{Résultats et analyses}
\label{sec:expe-analyses-plan}

\subsection{Baseline PLM (étape 0, protocole définitif)}
\label{sec:expe-baseline-distilbert}

La configuration de référence du tableau~\ref{tab:expe-ablation-flags}
(tous modules désactivés) est entraînée avec encodage \emph{document}
entier tronqué à 128 tokens et les hyperparamètres du
tableau~\ref{tab:expe-hyperparams}. Trois graines ($42$, $123$, $456$)
sont lancées pour \textbf{DistilBERT-base} \cite{sanh2019distilbert} et
\textbf{RoBERTa-base} \cite{liu2019roberta} ; le checkpoint retenu
maximise le F1-macro validation parmi les quatre époques. Le
tableau~\ref{tab:expe-baseline-step0} reporte les métriques test
(moyenne $\pm$ écart-type sur les trois graines) : référence interne pour H1,
comparaison externe prudente avec le F1-macro $0{,}46$ de BERT-base sur GoEmotions
\cite{demszky2020} (encodeur et fenêtre de contexte différents).
DistilBERT-base atteint un F1-macro test de $0{,}486 \pm 0{,}005$, RoBERTa-base
$0{,}487 \pm 0{,}005$ : les deux dépassent la référence BERT-base de
\cite{demszky2020} avec des écarts-types faibles entre graines. RoBERTa l'emporte
légèrement en F1-micro et en mAP ; l'écart macro reste inférieur à l'écart-type
inter-graines. Les ablations M1 à M4 seront donc menées sur \textbf{DistilBERT-base}
(budget GPU et lisibilité des comparaisons), RoBERTa servant de deuxième baseline
flat document dans le tableau~\ref{tab:expe-baseline-step0}.

\begin{table}[H]
\centering
\caption{Step~0 PLM (test, $n{=}5\,427$, protocole tableau~\ref{tab:expe-hyperparams}, 3 graines)}
\label{tab:expe-baseline-step0}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Encodeur} & \textbf{F1-micro} & \textbf{F1-macro} & \textbf{Exact match} & \textbf{mAP} \\
\hline
DistilBERT-base & $0{,}582 \pm 0{,}001$ & $0{,}486 \pm 0{,}005$ & $0{,}472 \pm 0{,}002$ & $0{,}506 \pm 0{,}001$ \\
\hline
RoBERTa-base & $0{,}586 \pm 0{,}003$ & $0{,}487 \pm 0{,}005$ & $0{,}473 \pm 0{,}002$ & $0{,}513 \pm 0{,}002$ \\
\hline
\end{tabular}
\end{table}

\subsection{Ablation M1 (encodage hiérarchique, H1)}
\label{sec:expe-m1-h1}

L'étape +M1 du tableau~\ref{tab:expe-ablation-flags} active la
segmentation en $M{=}4$ phrases (NLTK), l'encodage DistilBERT à
$32$ tokens par phrase et l'agrégation additive mot$\to$phrase puis
phrase$\to$document (chapitre~\ref{sec:conception-m1}), sans M2 à M4.
Le budget d'affinage reste celui du tableau~\ref{tab:expe-hyperparams}
(quatre époques, trois graines, sélection au F1-macro validation).
La comparaison porte sur la \textbf{même} baseline DistilBERT Step~0 :
cap de $128$ sous-mots, mais répartition phrase par phrase plutôt que
fenêtre document unique (voir section~\ref{sec:expe-corpus-stats}).

\begin{table}[H]
\centering
\caption{DistilBERT-base, test ($n{=}5\,427$, 3 graines) : Step~0 vs +M1}
\label{tab:expe-m1-distilbert}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Configuration} & \textbf{F1-micro} & \textbf{F1-macro} & \textbf{Exact match} & \textbf{mAP} \\
\hline
Step~0 PLM (flat) & $0{,}582 \pm 0{,}001$ & $0{,}486 \pm 0{,}005$ & $0{,}472 \pm 0{,}002$ & $0{,}506 \pm 0{,}001$ \\
\hline
+M1 (hiérarchique) & $0{,}564 \pm 0{,}002$ & $0{,}457 \pm 0{,}002$ & $0{,}450 \pm 0{,}005$ & $0{,}491 \pm 0{,}004$ \\
\hline
\end{tabular}
\end{table}

Le F1-macro test moyen passe de $0{,}486$ à $0{,}457$ ($\approx 2{,}9$
points absolus) ; les trois graines restent alignées (test macro
$0{,}457$, $0{,}454$, $0{,}459$). \textbf{H1 n'est pas confirmée} sous ce
protocole : l'encodage hiérarchique n'améliore pas la détection
multi-étiquettes macro par rapport au PLM document à encodeur fixé.
L'écart concerne aussi le F1-micro et l'exact match ; seule la lecture
flat avec troncature globale et pooling [CLS] performe mieux ici.
Cette observation motive la suite de l'ablation (M2 : attention
émotion$\to$phrases) plutôt qu'un prolongement direct de M1 seul.

\subsection{Ablation M1+M2 (attention par émotion, H2)}
\label{sec:expe-m1-m2-h2}

L'étape +M2 (tableau~\ref{tab:expe-ablation-flags}) conserve M1 et
remplace le pooling phrase$\to$document par une lecture croisée
multi-têtes par émotion sur $s_1,\dots,s_M$, un encodeur sur l'axe
des $K$ émotions et des têtes de classification par étiquette
(chapitre~\ref{sec:conception-m2}). Protocole d'affinage identique à
l'étape M1.

\begin{table}[H]
\centering
\caption{DistilBERT-base, test ($n{=}5\,427$, 3 graines) : +M1 vs +M1+M2 (H2)}
\label{tab:expe-m1-m2-distilbert}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Configuration} & \textbf{F1-micro} & \textbf{F1-macro} & \textbf{Exact match} & \textbf{mAP} \\
\hline
+M1 seul & $0{,}564 \pm 0{,}002$ & $0{,}457 \pm 0{,}002$ & $0{,}450 \pm 0{,}005$ & $0{,}491 \pm 0{,}004$ \\
\hline
+M1+M2 & $0{,}582 \pm 0{,}002$ & $0{,}505 \pm 0{,}004$ & $0{,}457 \pm 0{,}004$ & $0{,}504 \pm 0{,}002$ \\
\hline
\end{tabular}
\end{table}

Le F1-macro test passe de $0{,}457$ à $0{,}505$ ($\approx 4{,}8$ points
absolus) ; les trois graines convergent ($0{,}499$, $0{,}508$, $0{,}508$).
\textbf{H2 est confirmée} : l'attention croisée et les têtes par émotion
améliorent nettement la détection macro par rapport à M1 seul, avec un
F1-micro retrouvé au niveau du Step~0 flat ($0{,}582 \pm 0{,}002$).
La hiérarchie seule (M1) reste insuffisante ; le gain provient de la
lecture \emph{label-specific} sur les phrases. La branche M3 (NRC) et M4
testeront si un prior lexical et une pondération dynamique prolongent
cette ligne.

\subsection{Ablation M1+M2+M3 (NRC, H3)}
\label{sec:expe-m3-nrc-h3}

L'étape +M3 NRC injecte un vecteur documentaire à huit dimensions
(frequences d'affects NRCLex sur tokens couverts) dans les requêtes
d'émotions via la porte $g$ (initialisée à $0$, chapitre~\ref{sec:conception-m3}).
Protocole d'affinage inchangé.

\begin{table}[H]
\centering
\caption{DistilBERT-base, test ($n{=}5\,427$, 3 graines) : +M1+M2 vs +M3 NRC}
\label{tab:expe-m3-nrc-distilbert}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Configuration} & \textbf{F1-micro} & \textbf{F1-macro} & \textbf{Exact match} & \textbf{mAP} \\
\hline
+M1+M2 & $0{,}582 \pm 0{,}002$ & $0{,}505 \pm 0{,}004$ & $0{,}457 \pm 0{,}004$ & $0{,}504 \pm 0{,}002$ \\
\hline
+M1+M2+M3 (NRC) & $0{,}577 \pm 0{,}002$ & $0{,}504 \pm 0{,}008$ & $0{,}453 \pm 0{,}003$ & $0{,}504 \pm 0{,}001$ \\
\hline
\end{tabular}
\end{table}

Le F1-macro test moyen reste à $0{,}504$ ($\pm 0{,}008$), soit le niveau
de M1+M2 ($0{,}505 \pm 0{,}004$) : la moyenne sur trois graines n'indique
pas de gain net du prior NRC. Une graine atteint $0{,}515$ (seed $42$) tandis
que les deux autres restent sous $0{,}501$, d'où un écart-type plus élevé.
La porte apprise $|g|$ vaut environ $0{,}042$ en fin d'entraînement : le
modèle utilise une part modeste du signal NRC sans améliorer la macro
de façon stable par rapport à M1+M2.

\subsection{Ablation M1+M2+M3 (SenticNet, H3)}
\label{sec:expe-m3-senticnet-h3}

L'étape +M3 SenticNet injecte un vecteur documentaire à cinq dimensions
(polarités et couverture SenticNet~5, chapitre~\ref{sec:conception-choix})
dans les requêtes d'émotions via la même porte $g$ (initialisée à $0$).
Protocole d'affinage inchangé.

\begin{table}[H]
\centering
\caption{DistilBERT-base, test ($n{=}5\,427$, 3 graines) : +M1+M2 vs +M3 SenticNet}
\label{tab:expe-m3-senticnet-distilbert}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Configuration} & \textbf{F1-micro} & \textbf{F1-macro} & \textbf{Exact match} & \textbf{mAP} \\
\hline
+M1+M2 & $0{,}582 \pm 0{,}002$ & $0{,}505 \pm 0{,}004$ & $0{,}457 \pm 0{,}004$ & $0{,}504 \pm 0{,}002$ \\
\hline
+M1+M2+M3 (SenticNet) & $0{,}582 \pm 0{,}002$ & $0{,}504 \pm 0{,}003$ & $0{,}458 \pm 0{,}006$ & $0{,}515 \pm 0{,}002$ \\
\hline
\end{tabular}
\end{table}

Le F1-macro test moyen est $0{,}504$ ($\pm 0{,}003$), au niveau de M1+M2
($0{,}505 \pm 0{,}004$) et de la branche NRC ($0{,}504 \pm 0{,}008$).
Les trois graines restent proches ($0{,}505$, $0{,}499$, $0{,}507$), avec un
écart-type inter-graines plus faible que pour NRC. La porte $|g|$ vaut
environ $0{,}032$ : usage lexique légèrement plus faible qu'avec NRC.
Le F1-micro rejoint le Step~0 et M1+M2 ; le mAP gagne environ un point
par rapport à M1+M2 sans traduction nette en F1-macro.

\subsection{Synthèse H3 (NRC vs SenticNet)}
\label{sec:expe-h3-synthese}

\begin{table}[H]
\centering
\caption{DistilBERT-base, test ($n{=}5\,427$, 3 graines) : pile M1+M2 et variantes M3}
\label{tab:expe-h3-lexicons}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Configuration} & \textbf{F1-micro} & \textbf{F1-macro} & \textbf{Exact match} & \textbf{mAP} \\
\hline
+M1+M2 & $0{,}582 \pm 0{,}002$ & $0{,}505 \pm 0{,}004$ & $0{,}457 \pm 0{,}004$ & $0{,}504 \pm 0{,}002$ \\
\hline
+M1+M2+M3 (NRC) & $0{,}577 \pm 0{,}002$ & $0{,}504 \pm 0{,}008$ & $0{,}453 \pm 0{,}003$ & $0{,}504 \pm 0{,}001$ \\
\hline
+M1+M2+M3 (SenticNet) & $0{,}582 \pm 0{,}002$ & $0{,}504 \pm 0{,}003$ & $0{,}458 \pm 0{,}006$ & $0{,}515 \pm 0{,}002$ \\
\hline
\end{tabular}
\end{table}

\textbf{H3 est tranchée sur le plan comparatif} : ni NRC ni SenticNet
n'améliorent de façon robuste le F1-macro par rapport à M1+M2 ; les
moyennes sur trois graines coïncident à $\approx 0{,}504$--$0{,}505$.
SenticNet présente une variance inter-graines plus faible ; NRC peut
monter à $0{,}515$ sur une graine mais retombe sous $0{,}501$ sur les
autres. SenticNet préserve le F1-micro de M1+M2 et le mAP le plus élevé
des trois piles, sans gain macro décisif. Le levier principal reste
donc M2 ; M3 ajoute un prior appris modestement ($|g| \approx 0{,}03$
à $0{,}04$) plutôt qu'un saut de performance macro sur GoEmotions.

\subsection{Ablation M1+M2+M3+M4 (SenticNet, H4)}
\label{sec:expe-m4-h4}

H4 compare la pile M1+M2+M3 (SenticNet) à l'ajout de M4 : portes
$\gamma_k$ (équation~\ref{eq:dyngate}, $b_2{=}+3$) sur les $r_k$ avant
les têtes sigmoïdes, M3 inchangé. Protocole d'affinage identique.

\begin{table}[H]
\centering
\caption{DistilBERT-base, test ($n{=}5\,427$, 3 graines) : +M3 SenticNet vs +M4}
\label{tab:expe-m4-distilbert}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Configuration} & \textbf{F1-micro} & \textbf{F1-macro} & \textbf{Exact match} & \textbf{mAP} \\
\hline
+M1+M2+M3 (SenticNet) & $0{,}582 \pm 0{,}002$ & $0{,}504 \pm 0{,}003$ & $0{,}458 \pm 0{,}006$ & $0{,}515 \pm 0{,}002$ \\
\hline
+M1+M2+M3+M4 (SenticNet) & $0{,}577 \pm 0{,}002$ & $0{,}499 \pm 0{,}006$ & $0{,}452 \pm 0{,}003$ & $0{,}505 \pm 0{,}003$ \\
\hline
\end{tabular}
\end{table}

Le F1-macro test moyen passe de $0{,}504$ à $0{,}499$ ($\pm 0{,}006$) :
une graine atteint $0{,}507$ (seed $42$) mais les deux autres restent sous
$0{,}496$, d'où une moyenne inférieure à M3 SenticNet et à M1+M2
($0{,}505 \pm 0{,}004$). Le F1-micro et le mAP reculent légèrement.
\textbf{H4 n'est pas confirmée} : la pondération dynamique n'améliore pas
robustement la macro sur GoEmotions dans ce cadre. La configuration la
plus efficace en F1-macro test reste \textbf{M1+M2} sans lexique ni M4.

\subsection{Synthèse des hypothèses H1 à H4}
\label{sec:expe-hypotheses-synthese}

\begin{table}[H]
\centering
\caption{DistilBERT-base, F1-macro test (moyenne $\pm$ écart-type, 3 graines)}
\label{tab:expe-h1-h4-summary}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{|l|c|c|}
\hline
\textbf{Hypothèse} & \textbf{Comparaison} & \textbf{F1-macro test} \\
\hline
H1 & Step~0 flat vs +M1 & $0{,}486 \pm 0{,}005$ vs $0{,}457 \pm 0{,}002$ (H1 rejetée) \\
\hline
H2 & +M1 vs +M1+M2 & $0{,}457 \pm 0{,}002$ vs $0{,}505 \pm 0{,}004$ (H2 confirmée) \\
\hline
H3 & +M1+M2 vs M3 (NRC / SenticNet) & $0{,}505 \pm 0{,}004$ vs $0{,}504 \pm 0{,}008$ / $0{,}504 \pm 0{,}003$ (pas de gain macro robuste) \\
\hline
H4 & +M3 SenticNet vs +M4 & $0{,}504 \pm 0{,}003$ vs $0{,}499 \pm 0{,}006$ (H4 non confirmée) \\
\hline
\end{tabular}
\end{table}

\subsection{Runs exploratoires (hyperparamètres non finaux)}
\label{sec:expe-baseline-pilot}

Des entraînements préliminaires (budget en époques ou taux d'apprentissage
non alignés sur \cite{demszky2020}) ont validé le pipeline Colab ;
ils ne servent pas de référence pour H1 ni pour le
tableau~\ref{tab:expe-baseline-step0}.

\subsection{Analyses complémentaires (en cours)}
\label{sec:expe-analyses-suite}

Les éléments suivants compléteront ce chapitre en analyse fine :
\begin{itemize}
\item F1 par classe et matrices de confusion (frontière avec
      \emph{neutral}, classes quasi vides) ;
\item exemples qualitatifs sur émotions rares.
\end{itemize}
Les hypothèses H1 à H4 sont récapitulées au
tableau~\ref{tab:expe-h1-h4-summary}.

\section{Points de vigilance méthodologiques}
\label{sec:expe-lecons}

Avant de tirer des conclusions, les points suivants seront contrôlés :
\begin{itemize}
\item \textbf{Troncature} : suivre le taux mesuré en
      section~\ref{sec:expe-corpus-stats} ; toute modification de
      $M$ ou de 32 tokens impose de refaire cette analyse.
\item \textbf{Initialisation} : respecter $g{=}0$ et $b_2{=}+3$ pour
      M3/M4 (chapitre~\ref{chap:conception}) ;
\item \textbf{Budget} : ne jamais comparer des configurations à lot ou
      époques différents sans le signaler ;
\item \textbf{Graines} : privilégier plusieurs graines avant de conclure
      sur H1 à H4.
\end{itemize}

\section*{Conclusion}
\addcontentsline{toc}{section}{Conclusion}
Le protocole expérimental est spécifié et exécuté sur DistilBERT-base :
corpus GoEmotions, statistiques de segmentation, hyperparamètres communs
et ladder M1 à M4. Le Step~0 PLM couvre DistilBERT et RoBERTa
(tableau~\ref{tab:expe-baseline-step0}) ; H1 à H4 sont reportées aux
tableaux d'ablation et au tableau~\ref{tab:expe-h1-h4-summary}.
La pile retenue pour la macro test est M1+M2 ; M3 et M4 n'apportent pas
de gain robuste au-delà de M2. Les analyses par classe restent à
mener (section~\ref{sec:expe-analyses-suite}).
